<!DOCTYPE html>
<html>
	<head>
		<meta charset='utf-8'>
		<link rel="stylesheet" href="/css/sobaka.css" />
		<title>Error!</title>
	</head>
	<body>
		<main>
			<header>
				<div>{{!it.error}}</div>
				<div>{{!it.message}}</div>
				{{?it.debug && it.stack}}
					<pre>{{=it.stack}}</pre>
				{{?}}
			</header>
		</main>
	</body>
</html>
